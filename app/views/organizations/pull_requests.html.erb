<h1>
  <%= @org %> - Repositories and Pull Requests
</h1>

<br>
<% @repos.each do |repo| %>
  <br>
  <br>
  <strong>
    <%= repo[:name] %>
  </strong>

  <% repo[:pull_requests].each do |pr| %>
    <br> <%= link_to "#{pr[:number]} - #{pr[:title]}", pr[:html_url] %>
  <% end %>

<% end %>
