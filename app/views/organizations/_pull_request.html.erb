<%= link_to pr[:html_url], class: "list-group-item"  do %>
  <div class="row">
    <div class="col-xs-10">
      <%= "##{pr[:number]} - #{pr[:title]}" %>
    </div>
    <div class="col-xs-2">
      <%= image_tag pr[:user][:avatar_url], class: "pr-avatar pull-right" %>
    </div>
  </div>
<% end %>
